<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>signup</title>
</head>
<style>
  .form {
    display: flex;
    flex-direction: column;
    gap: 10px;
    max-width: 350px;
    padding: 20px;
    border-radius: 20px;
    position: relative;
    background-color: transparent;
    color: #fff;
    border: 1px solid #333;
    justify-content: center;
  }

  .title {
    font-size: 28px;
    font-weight: 600;
    letter-spacing: -1px;
    position: relative;
    display: flex;
    align-items: center;
    padding-left: 30px;
    color: rgb(118, 206, 206);
  }

  .title::before {
    width: 18px;
    height: 18px;
  }

  .title::after {
    width: 18px;
    height: 18px;
    animation: pulse 1s linear infinite;
  }

  .title::before,
  .title::after {
    position: absolute;
    content: "";
    height: 16px;
    width: 16px;
    border-radius: 50%;
    left: 0px;
    background-color: aqua;
  }

  .message,
  .signin {
    font-size: 14.5px;
    color: rgba(255, 255, 255, 0.7);
  }

  .signin {
    text-align: center;
  }

  .signin a:hover {
    text-decoration: underline royalblue;
  }

  .signin a {
    color: #00bfff;
  }

  .flex {
    display: flex;
    width: 100%;
    gap: 25px;

  }

  .form label {
    position: relative;
  }

  .form label .input {
    background-color: transparent;
    color: #fff;
    width: 100%;
    padding: 20px 05px 05px 10px;
    outline: 0;
    border: 1px solid rgba(105, 105, 105, 0.397);
    border-radius: 10px;
  }

  .form label .input+span {
    color: rgba(255, 255, 255, 0.5);
    position: absolute;
    left: 10px;
    top: 0px;
    font-size: 0.9em;
    cursor: text;
    transition: 0.3s ease;
  }

  .form label .input:placeholder-shown+span {
    top: 12.5px;
    font-size: 0.9em;
  }

  .form label .input:focus+span,
  .form label .input:valid+span {
    color: #00bfff;
    top: 0px;
    font-size: 0.7em;
    font-weight: 600;
  }

  .input {
    font-size: medium;
  }

  .submit {
    box-shadow: 6px 8px 10px #5387a8;
    background-color: rgb(81, 108, 139);
    border: 1px solid aqua;
    outline: none;
    padding: 10px;
    border-radius: 10px;
    color: #fff;
    font-size: 16px;
    outline: none;
    text-decoration: none;
    display: flex;
    justify-content: center;
    transform: .3s ease;
  }

  .submit:hover {
    border: none;
    box-shadow: 2px 5px 7px #335468;
    transform: scale(1.09);
    transition: .5s;
    transition-property: all;
    background-color: rgb(55, 62, 82);
    color: white;
    animation: ease-in-out;
  }

  @keyframes pulse {
    from {
      transform: scale(0.9);
      opacity: 1;
    }

    to {
      transform: scale(1.8);
      opacity: 0;
    }
  }

  .form {
    background: transparent;
    border: 1px solid #00bfff;
  }

  .form:hover {
    transform: scale(1.05);
    transition: .5s;
    animation: ease-in-out;
    box-shadow: 2px 8px 5px 7px #335468;
  }

  .form label .input+span {
    color: rgba(255, 255, 255, 0.5);
    position: absolute;
    left: 10px;
    top: 0px;
    font-size: 0.9em;
    cursor: text;
  }

  label:hover {
    transform: scale(1.02);
    transition: .5s;
    animation: ease-in-out;
  }
</style>

<body
  style="background: linear-gradient(100deg,rgb(40, 40, 78),rgb(24, 78, 78)); display: flex; justify-content: center; padding-top: 50px; font-family: Arial, Helvetica, sans-serif;">
  <form class="form" name="form" method="post" action="/signup">
    <p class="title">Register </p>
    <p class="message">Signup now and get full access to our Movies. </p>
    <div class="flex">
      <label>
        <input class="input" id="first" type="text" placeholder="" required="" name="Firstname">
        <span>Firstname</span>
      </label>

      <label>
        <input class="input" id="last" type="text" placeholder="" required="" name="Lastname">
        <span>Lastname</span>
      </label>
    </div>

    <label>
      <input class="input" id="email" type="email" placeholder="" required="" name="email">
      <span>Email</span>
    </label>
    <label>
      <input class="input" id="user" type="text" placeholder="" required="" name="Username">
      <span>Username</span>
    </label>
    <label>
      <input class="input" id="pass1" type="password" placeholder="" required="" name="password">
      <span>Password</span>
    </label>
    <label>
      <input class="input" id="pass" type="password" placeholder="" required="" name="Confirmpassword">
      <span>Confirm password</span>
    </label>
    <button class="submit">Submit </button>
    <p class="signin">Already have an acount ? <a href="/login">Signin</a> </p>
  </form>
<script>
  var Firstname = document.forms['form']['Firstname'];
  var Lastname = document.forms['form']['Lastname'];
  var email = document.forms['form']['email'];
  var Username = document.forms['form']['Username'];
  var password = document.forms['form']['password'];
  var Confirmpassword = document.forms['form']['Confirmpassword'];

  var firstName_error = document.getElementById('first')
  var lastName_error = document.getElementById('last')
  var email_error = document.getElementById('email')
  var userName_error = document.getElementById('user')
  var password_error = document.getElementById('pass1')
  var confirmPassword_error = document.getElementById('pass')
  function validate(){
    if(email.value.length < 9){
      email.style.border = "1px solid red"
      email_error.style.display = "block"
      email.focus();
      return false;
    }
  }
</script>


</body>

</html>